<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fitflo.fitflo">

    <!-- needed for networking permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EventDetailsActivity"
            android:label="@string/title_activity_event_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegistrationActivity"
            android:label="@string/title_activity_registration"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".CreateEventActivity"
            android:label="@string/title_activity_create_event"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MyEventsActivity"
            android:label="@string/title_activity_my_events"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SelectSkillsActivity"
            android:label="@string/title_activity_select_skills"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".InstructorRegistrationActivity"
            android:label="@string/title_activity_instructor_registration"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DateSelectActivity"
            android:label="@string/title_activity_date_select"
            android:theme="@style/AppTheme.PopUpTheme" />
        <activity
            android:name=".AdvancedSearchActivity"
            android:label="@string/title_activity_advanced_search"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CoachesProfile"
            android:label="@string/title_activity_coaches_profile"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
